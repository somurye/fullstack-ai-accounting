/**
-- ============================================================================
-- labor-standards.constants.ts
-- 労働基準法および社内労務規定に基づく労働時間計算定数の一元定義
-- (将来の法改正・改定、P3-T3 給与計算での再利用に備えて一箇所に集約)
-- ============================================================================
*/

export const LABOR_STANDARDS = {
  /** 1日の法定労働時間（分）: 8時間 = 480分 */
  DAILY_REGULAR_LIMIT_MINUTES: 8 * 60,

  /** 1日の法定労働時間（時間単位） */
  DAILY_REGULAR_LIMIT_HOURS: 8.0,

  /** 1週間の法定労働時間（時間単位） */
  WEEKLY_REGULAR_LIMIT_HOURS: 40.0,

  /** 深夜労働の開始時刻 (時: 22時) */
  LATE_NIGHT_START_HOUR: 22,

  /** 深夜労働の終了時刻 (時: 翌朝5時) */
  LATE_NIGHT_END_HOUR: 5,

  /**
   * 将来の給与計算 (P3-T3) 用の割増率定義
   * ※法改正や労使協定による改定時に本定数を変更することで全体に反映可能
   */
  PREMIUM_RATES: {
    /** 時間外労働（法定8時間超）割増率: 25% (0.25) */
    OVERTIME: 0.25,

    /** 深夜労働（22:00〜05:00）割増率: 25% (0.25) */
    LATE_NIGHT: 0.25,

    /** 休日労働（法定休日）割増率: 35% (0.35) */
    HOLIDAY: 0.35,

    /** 時間外労働が月60時間を超える部分の割増率: 50% (0.50) */
    OVERTIME_OVER_60H: 0.50,
  },
} as const;
